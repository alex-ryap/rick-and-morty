query character($id: ID!) {
  character(id: $id) {
    id
    name
    image
    status
    species
    gender
    origin {
      name
      type
    }
    location {
      name
      type
    }
    episode {
      name
      air_date
      episode
    }
  }
}
