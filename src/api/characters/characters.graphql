query allCharacters($page: Int) {
  characters(page: $page) {
    info {
      count
      pages
    }
    results {
      id
      name
      image
      gender
      location {
        name
      }
    }
  }
}
