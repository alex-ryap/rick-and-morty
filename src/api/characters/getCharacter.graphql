query character($id: ID!) {
  character(id: $id) {
    id
    name
    image
    status
    species
    gender
    origin {
      id
      name
      type
    }
    location {
      id
      name
      type
    }
    episode {
      id
      name
      air_date
      episode
    }
  }
}
